[package]
name = "drduc-translator-desktop"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
rfd = "0.15"
tauri = { version = "2", features = [] }

[features]
default = []
